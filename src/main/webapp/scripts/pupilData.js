/**
 * Created by slavkosoltys on 16.06.17.
 */
function getTeacherData(id) {
    alert("teacher Id - " + id)
} //TODO: get teacher data

function readHomework(homework) {
    alert("homework")
} //TODO: show full homework


// $short_string = (strlen($string) > 30) ? substr($string,0,300).'...' : $string;
